# 脅威モデル(Threat Model)

HushBoxが「何を守り、何を想定し、どう対策しているか」と、逆に「何は守らないか」を明文化したものです。導入を検討する保護者・先生・学校のIT管理者向けの判断材料です。

学校等の組織で導入する場合は、本ドキュメントを踏まえた追加のセキュリティレビューを推奨します。HushBoxは個人・家庭・小規模な教育現場向けの教育目的OSSであり、企業のセキュリティ製品と同等の保証はしません。

## 守りたいもの

- 生徒の会話内容(チャットの入力・AIの応答)
- 生徒の個人情報(氏名・住所・電話番号・学校名・SNS ID等)
- 生徒がPCを日常的に安全に使えること

## 攻撃者・脅威と対策

| # | 脅威 | 対策 | 残るリスク |
|---|---|---|---|
| 1 | AI提供会社への会話送信 | 完全ローカル推論(Candle)。`network_boundary_test.rs`で通信コードの混入を検証 | ローカル推論である以上、原則発生しない |
| 2 | ネットワーク盗聴 | 推論時は通信なし。モデルダウンロードのみHTTPS | ダウンロード先はhuggingface.co系に限定 |
| 3 | AI/SNS相手からの個人情報の聞き出し | `pii_guard.rs`によるPII検出・匿名化、`safety_drill.rs`による訓練 | 検出はヒューリスティックで完全ではない(既知の穴は`pii_guard.rs`参照) |
| 4 | 不適切なプロンプトへの応答 | `safety_policy.rs`がLLM呼び出し前に決定的に判定し、危険時は固定応答 | ローカルモデル特有の予測不能な出力は完全には防げない |
| 5 | モデルファイルの改ざん | SHA-256 pin(`ModelSpec::expected_sha256`)を照合し不一致ならロード拒否 | アプリのバイナリごと改ざんされた場合は無力(#8参照) |
| 6 | PC上の別のマルウェア | **原則として防げない**。OSレベルの隔離機能を超えた保護はしない | OS自体のセキュリティに委ねられる |
| 7 | 同じPCを使う家族・管理者によるローカルデータ閲覧 | 会話履歴はメモリ上のみで永続化しない。モデルキャッシュに個人情報は含まない | 同一OSアカウントなら原理的にアクセス可能 |
| 8 | 配布物自体の改ざん | 未対策(コード署名は未導入)。ソースは公開済み | 配布物単体では真正性を検証できない。信頼できる入手経路を推奨 |
| 9 | WebView経由の攻撃(XSS等) | Tauri Capabilities機構で権限を`capabilities/default.json`に限定。CSPで`script-src 'self'`等を明示 | XSS自体の発生は防げない多層防御に留まる |
| 10 | サプライチェーン攻撃 | CIで`cargo audit`・`npm audit`を実行。`Cargo.lock`で固定 | 既知の脆弱性のみ検出可能 |

## 何を守れないか(平易な説明)

- **PCが既にウイルスに感染している場合**は防げません。ウイルス対策とOS更新は別途必要です。
- **生徒本人が個人情報を書いて「それでも送る」を選んだ場合**、送信は止められません。
- **同じPCを使う家族や管理者権限を持つ人**からは、原理的にデータが見られます。
- **配布されたインストーラー自体の改ざん**は、現状検証できません。

## 変更履歴

- 会話ログの永続化・アップデーター・コード署名等を追加する際は、該当する行を必ず更新してください。
