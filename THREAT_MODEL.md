# 脅威モデル(Threat Model)

このドキュメントは、HushBoxが「何を守るために、何を想定して、どう対策しているか」と、
逆に「何は守らないか」を明文化するものです。保護者・先生・学校のIT管理者が
導入を検討する際、README(セキュリティモデル)だけでは伝わりにくい
「結局どこまで安全なのか」を判断する材料として使ってください。

READMEにも記載の通り、**学校等の組織で導入する場合は、本ドキュメントを踏まえた
追加のセキュリティレビューを推奨します**。HushBoxは個人・家庭・小規模な教育現場での
利用を想定した教育目的のOSSであり、企業のセキュリティ製品と同等の保証はしません。

## 想定する資産(守りたいもの)

- 生徒の会話内容(チャットの入力・AIの応答)
- 生徒の個人情報(氏名・住所・電話番号・学校名・SNS ID等)
- 生徒がPCを日常的に安全に使えること(マルウェア感染や不正操作を招かないこと)

## 想定する攻撃者・脅威と対策

| # | 攻撃者/脅威 | 想定するシナリオ | 対策 | 残るリスク |
|---|---|---|---|---|
| 1 | AI/モデル提供会社 | 会話内容が外部のAIサービスに送信され、学習データ等に利用される | 完全ローカル推論(Candle)。初回のモデルダウンロード以外、外部通信を行わない設計。`network_boundary_test.rs`で通信コードの混入を機械的に検証 | ローカル推論である以上、この脅威は原則発生しない(モデルダウンロード自体はHugging Faceに1回だけ通信する) |
| 2 | ネットワーク盗聴者(同一Wi-Fi上の第三者等) | 通信内容を盗聴・改ざんする | 推論時は通信自体が発生しないため盗聴対象が無い。モデルダウンロードはHTTPS | 初回ダウンロード時のTLS証明書検証はOS/reqwestの標準実装に依存。ダウンロード先ドメインはコードでhuggingface.co系に限定 |
| 3 | 悪意あるWebサイト・SNSの相手(生徒がAIやSNSの相手に個人情報を聞き出されようとする) | 「名前を教えて」「住所は?」のようなソーシャルエンジニアリング的な誘導 | `pii_guard.rs`によるPII検出・匿名化(送信前・送信直前の多層防御)、`safety_drill.rs`による「聞かれたときにどう断るか」の訓練 | 検出は正規表現・辞書ベースのヒューリスティックであり完全ではない(既知の穴はREADME/`pii_guard.rs`のコメント参照)。最終的には生徒自身の判断力に依存する部分がある |
| 4 | 悪意あるプロンプト(生徒自身、または生徒が第三者から渡された文面) | AIに不適切な内容(暴力・自傷・性的内容・危険物の作り方等)を答えさせようとする | `safety_policy.rs`でLLM呼び出し前にRust側が決定的に判定し、危険と判定した場合はLLMを呼ばず固定応答を返す(system promptの指示だけに頼らない) | 1.5B〜7B規模のローカルモデル特有の予測不能な出力(ハルシネーション、意図しない不適切表現)自体を完全には防げない。安全ポリシーのキーワード網羅にも限界がある |
| 5 | 改ざんされた/差し替えられたモデルファイル | ダウンロード時や配布時にモデルファイルが不正なものに差し替えられる | ビルド時に埋め込んだSHA-256 pin(`ModelSpec::expected_sha256`)と実際にダウンロードしたファイルのハッシュを照合し、不一致ならロード自体を拒否 | pin自体はアプリのソースコード内に平文で埋め込まれているため、アプリのバイナリごと改ざんされた場合(下記#8参照)は無力 |
| 6 | PC上で動く別のマルウェア(生徒が別経路で感染させてしまった等) | メモリ上の会話内容を読み取る、ファイルを盗む、キーロガーで入力を盗む等 | **原則として防げない**。HushBoxはOSプロセスの一つに過ぎず、OSレベルの隔離機能を超えた保護は提供しない | OS自体のセキュリティ(ウイルス対策ソフト、OSの更新状況等)に委ねられる。この点はどのデスクトップアプリでも共通の限界 |
| 7 | 保護者/学校管理者/同じPCを使う他の家族 | ローカルに残るデータ(モデルキャッシュ、将来的な会話ログ等)を閲覧する | 会話履歴は現状メモリ上のみで永続化しない(アプリを終了すれば消える)。モデルキャッシュ(`~/.cache/huggingface/hub-simple`)は公開モデルのファイルのみで、個人情報は含まない | **OSのファイルアクセス権に依存**。同一OSユーザーアカウントを共有している場合、原理的には他の家族もアクセスできる。将来、会話ログの永続化機能を追加する場合はこの表の更新が必須 |
| 8 | 配布物自体の改ざん(第三者が偽のインストーラーを配布する等) | 「HushBoxです」と称する改ざん済みバイナリを生徒がインストールしてしまう | 現状、コード署名は未導入(README「動作状況」参照)。ソースコードは公開されており、ビルド元の確認は可能 | **現状、配布物単体では真正性を検証する手段が無い**。学校等で導入する場合は、信頼できる経路(公式リポジトリからの自前ビルド等)で入手することを推奨。コード署名の導入は今後の課題 |
| 9 | フロントエンド(WebView)の脆弱性経由の攻撃(XSS等) | 何らかの経路で悪意あるスクリプトがWebView内で実行され、Tauri IPC経由でRust側の機能を不正に呼び出す | Tauri v2のCapabilities機構により、フロントエンドから呼び出せる権限を`capabilities/default.json`に列挙したもの(イベントの listen/unlisten/emit 等)のみに限定。CSP(`tauri.conf.json`)で `script-src 'self'` 等を明示し、外部スクリプトの読み込みや `eval` 系実行を制限。外部URLを開く機能(`shell:allow-open`)は未使用だったため権限ごと削除済み | CSP・Capabilitiesは「起こりうる被害の範囲」を狭める多層防御であり、XSS自体の発生(例えばReactの`dangerouslySetInnerHTML`の誤用等)を防ぐものではない。フロントエンドコードのレビューは引き続き必要 |
| 10 | サプライチェーン攻撃(依存クレート/npmパッケージの脆弱性・悪意あるパッケージ) | 依存関係経由でコードが混入する | CI(`.github/workflows/ci.yml`)で`cargo audit`・`npm audit --audit-level=high`を実行し、既知の脆弱性を機械的に検出。`Cargo.lock`をコミットし`cargo test --locked`でロックファイル無視の混入を防止 | 既知(登録済み)の脆弱性のみ検出可能。ゼロデイや悪意ある新規パッケージそのものは検出できない |

## 「何を守れないか」のまとめ(保護者・先生向けの平易な説明)

- **このPC自体が既にウイルスに感染している場合**、HushBoxはそれを防げません。ウイルス対策ソフトとOSの更新は別途必要です。
- **生徒本人が意図的に個人情報を書いた場合**、アプリは気づかせようとしますが(警告・マスキング)、最終的に「それでも送る」を選べば送信は止められません(AIへの送信自体はこの端末内で完結しますが、匿名化されない生の情報がAIに渡ります)。
- **同じPCを使う家族や、そのPCの管理者権限を持つ人**からは、原理的にはこのアプリのデータも見られます(OSのアクセス権の話であり、アプリ固有の弱点ではありません)。
- **配布されたインストーラー自体が改ざんされていないか**は、現状HushBox自身では検証できません。信頼できる入手経路をご利用ください。

## 変更履歴

- この脅威モデルは、外部AIによる複数のコードレビュー(claude/GPT/deepseek/kimi)での指摘を踏まえて初版を作成しました。今後、会話ログの永続化・アップデーター・コード署名等の機能を追加する際は、該当する行を必ず更新してください。
